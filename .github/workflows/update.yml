name: Daily System Info Update

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'profile/README.md'
      - '.github/stats.json'
  schedule:
    # Run everyday at 00:00 UTC
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
    permissions:
      contents: write
    steps:
      - name: Install required packages
        run: |
          pacman -Sy --noconfirm git python

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run update script
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          python .github/scripts/update_readme.py

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/README.md .github/stats.json
          git diff --staged --quiet || (git commit -m "chore: update system info & uptime" && git push)
